# NotYourResume — environment template (copy to .env.local; never commit real values)
# Supabase (Part 16: [decided] — Postgres, Auth, RLS, Storage)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=        # server-only; never exposed to the client
# AI gateway (Part 17: Claude, server-side only, one module; app must run with this UNSET via stubs)
ANTHROPIC_API_KEY=
# Stripe Checkout (Part 3.3: one flat founding charge; statement descriptor NOTYOURRESUME)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
# Email nudges (Part 16 [recommendation]: Resend or Postmark)
RESEND_API_KEY=
# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
